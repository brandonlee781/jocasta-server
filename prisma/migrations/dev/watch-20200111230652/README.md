# Migration `watch-20200111230652`

This migration has been generated by Brandon Lee at 1/11/2020, 11:06:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "jocasta_schema"."Character" (
  "background" text    ,
  "characteristics" text  NOT NULL  REFERENCES "jocasta_schema"."Characteristic"("id") ON DELETE RESTRICT,
  "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "credits" integer    ,
  "derivedAttributes" text  NOT NULL  REFERENCES "jocasta_schema"."DerivedAttributes"("id") ON DELETE RESTRICT,
  "description" text    ,
  "encumberance" text    REFERENCES "jocasta_schema"."Threshold"("id") ON DELETE SET NULL,
  "experience" text    REFERENCES "jocasta_schema"."Experience"("id") ON DELETE SET NULL,
  "id" text  NOT NULL  ,
  "imageUrl" text    ,
  "injuries" text    ,
  "motivation" text    ,
  "name" text  NOT NULL DEFAULT '' ,
  "notes" text    ,
  "resources" text    ,
  "species" text  NOT NULL DEFAULT '' ,
  "system" text  NOT NULL DEFAULT 'AOR' ,
  "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "user" text  NOT NULL  REFERENCES "jocasta_schema"."User"("id") ON DELETE RESTRICT,
  PRIMARY KEY ("id")
);

ALTER TABLE "jocasta_schema"."Armor" ADD COLUMN "character" text    REFERENCES "jocasta_schema"."Character"("id") ON DELETE SET NULL,
DROP COLUMN "condition",
ADD COLUMN "condition" text    ,
DROP COLUMN "description",
ADD COLUMN "description" text    ;

ALTER TABLE "jocasta_schema"."CharacterSkill" ADD COLUMN "character" text    REFERENCES "jocasta_schema"."Character"("id") ON DELETE SET NULL;

ALTER TABLE "jocasta_schema"."CharacterTalent" ADD COLUMN "character" text    REFERENCES "jocasta_schema"."Character"("id") ON DELETE SET NULL;

ALTER TABLE "jocasta_schema"."Duty" ADD COLUMN "character" text    REFERENCES "jocasta_schema"."Character"("id") ON DELETE SET NULL;

ALTER TABLE "jocasta_schema"."Gear" ADD COLUMN "character" text    REFERENCES "jocasta_schema"."Character"("id") ON DELETE SET NULL,
DROP COLUMN "description",
ADD COLUMN "description" text    ;

ALTER TABLE "jocasta_schema"."Specialization" ADD COLUMN "character" text    REFERENCES "jocasta_schema"."Character"("id") ON DELETE SET NULL;

ALTER TABLE "jocasta_schema"."Talent" DROP COLUMN "description",
ADD COLUMN "description" text    ,
DROP COLUMN "page",
ADD COLUMN "page" text    ;

ALTER TABLE "jocasta_schema"."TalentTreeLocation" DROP COLUMN "connections",
ADD COLUMN "connections" integer []   ;

ALTER TABLE "jocasta_schema"."User" DROP COLUMN "name",
ADD COLUMN "name" text    ;

ALTER TABLE "jocasta_schema"."Weapon" ADD COLUMN "character" text    REFERENCES "jocasta_schema"."Character"("id") ON DELETE SET NULL,
DROP COLUMN "condition",
ADD COLUMN "condition" text    ,
DROP COLUMN "description",
ADD COLUMN "description" text    ,
DROP COLUMN "make",
ADD COLUMN "make" text    ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20200111230222..watch-20200111230652
--- datamodel.dml
+++ datamodel.dml
@@ -3,16 +3,17 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql//brandonleeSrf32pstgrs@localhost5432/jocasta?schema=jocasta_schema"
 }
 model User {
-  id       String  @default(cuid()) @id
-  email    String  @unique
-  password String
-  name     String?
+  id          String      @default(cuid()) @id
+  email       String      @unique
+  password    String
+  name        String?
+  characters  Character[]
 }
 enum GearCondition {
   NONE
@@ -129,8 +130,36 @@
   defense DefenseAttribute
 }
 // Character
+model Character {
+  id                  String            @default(cuid()) @id
+  name                String
+  system              System
+  species             String
+  specializations     Specialization[]
+  characteristics     Characteristic
+  derivedAttributes   DerivedAttributes
+  skills              CharacterSkill[]
+  talents             CharacterTalent[]
+  gear                Gear[]
+  weapons             Weapon[]
+  armor               Armor[]
+  encumberance        Threshold?
+  experience          Experience?
+  resources           String?
+  credits             Int?
+  injuries            String?
+  background          String?
+  description         String?
+  motivation          String?
+  duties              Duty[]
+  imageUrl            String?
+  notes               String?
+  user                User
+  createdAt           DateTime          @default(now())
+  updatedAt           DateTime          @updatedAt
+}
 // Gear
 model Gear {
   id            String  @default(cuid()) @id
```


